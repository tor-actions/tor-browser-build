This is the port of Apple's libdispatch.
We pull it as a dependency of cctools, like Firefox does.

So, the build script is an adaptation of Mozilla's
`taskcluster/scripts/misc/build-cctools-port.sh`.
